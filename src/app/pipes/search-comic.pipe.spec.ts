import { SearchComicPipe } from './search-comic.pipe';

describe('SearchComicPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchComicPipe();
    expect(pipe).toBeTruthy();
  });
});
