import { SearchCreatorPipe } from './search-creator.pipe';

describe('SearchCreatorPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCreatorPipe();
    expect(pipe).toBeTruthy();
  });
});
