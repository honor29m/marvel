import { SearchSeriesPipe } from './search-series.pipe';

describe('SearchSeriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
